server:
  port: 8080
  host: 0.0.0.0
sessions:
  scan_interval: 30s
  output_buffer_size: 10MB
  default_dir: ~/projects
notifications:
  desktop: true
  events: [completed, errored, waiting]
auth:
  enabled: false
  token: ""

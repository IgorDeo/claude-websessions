name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install templ
        run: go install github.com/a-h/templ/cmd/templ@v0.3.1001

      - name: Generate templates
        run: templ generate ./web/templates/

      - name: Run tests
        run: go test ./... -v

  build:
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            suffix: linux-amd64
          - goos: linux
            goarch: arm64
            suffix: linux-arm64
          - goos: darwin
            goarch: amd64
            suffix: darwin-amd64
          - goos: darwin
            goarch: arm64
            suffix: darwin-arm64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install templ
        run: go install github.com/a-h/templ/cmd/templ@v0.3.1001

      - name: Generate templates
        run: templ generate ./web/templates/

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
          VERSION: ${{ github.ref_name }}
          SUFFIX: ${{ matrix.suffix }}
        run: |
          go build -ldflags="-s -w -X main.version=${VERSION}" \
            -o "websessions-${SUFFIX}" \
            ./cmd/websessions

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: websessions-${{ matrix.suffix }}
          path: websessions-${{ matrix.suffix }}

  build-gui:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Install templ
        run: go install github.com/a-h/templ/cmd/templ@v0.3.1001

      - name: Generate templates
        run: templ generate ./web/templates/

      - name: Install WebKit2GTK dev libs
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev

      - name: Build webview helper
        run: make build-webview-helper

      - name: Build GUI binary
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          go build -tags gui -ldflags="-s -w -X main.version=${VERSION}" \
            -o "websessions-gui-linux-amd64" \
            ./cmd/websessions

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: websessions-gui-linux-amd64
          path: websessions-gui-linux-amd64

  release:
    needs: [build, build-gui]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Prepare release files
        run: |
          mkdir -p release
          for dir in dist/*/; do
            cp "$dir"* release/
          done
          chmod +x release/websessions-*
          cd release
          for f in websessions-*; do
            sha256sum "$f" >> checksums.txt
          done

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            release/websessions-*
            release/checksums.txt
